# Thesis (LaTeX)

LaTeX sources for the thesis document.

## Quick start
1. Install a LaTeX distribution (Texlive, MacTeX, or MiKTeX).
2. From this directory:
   - `make`
   - or `latexmk --lualatex thesis.tex --shell-escape --silent`
3. Edit `thesis.tex` and files in `chapters/` and `abstract/`.
